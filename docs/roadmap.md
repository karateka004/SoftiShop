# roadmap.md — фазы

Правило: **одна фаза за раз**, в конце — СТОП и ожидание «ОК». В конце фазы — обновить
`progress.md` и этот файл, сделать git-commit.

- [x] **Фаза 0** — каркас Next.js + Tailwind v4 + shadcn/ui + git, папки, `CLAUDE.md` и `/docs`,
  форма mock-данных, абстракция `lib/commerce.ts`, подготовка к Vercel. *Токены одобрены.*
- [x] **Фаза 1** — дизайн-токены (`@theme`) + базовый layout: header, footer, навигация.
  circular-font исправлен; шрифты **Pirata One / Oswald / Golos Text**. *Подтверждено визуально.*
- [x] **Фаза 2** — главная: hero + промо-секции (value-props, manifesto, order-CTA). *Готово.*
- [x] **Фаза 3** — страница коллекции `/products` (сетка) + карточка товара + фото в `public/products/`. *Готово.*
- [ ] **Фаза 4** — страница товара `/products/[handle]`: галерея, размерная таблица,
  «легенда», выбор размера (визуально), добавление в клиентскую корзину.
- [ ] **Фаза 5** — корзина (drawer) на клиентском состоянии; вместо чекаута — CTA «Замовити в Direct».
- [ ] **Фаза 6** — страницы About / FAQ / Lookbook.
- [ ] **Фаза 7** — адаптив-полировка, SEO (метаданные, Open Graph), перформанс, a11y,
  деплой на Vercel → публичный URL для показа.

Легенда: `[ ]` не начато · `[~]` в работе/на ревью · `[x]` готово.
