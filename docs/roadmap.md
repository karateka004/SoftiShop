# roadmap.md — фазы

Правило: **одна фаза за раз**, в конце — СТОП и ожидание «ОК». В конце фазы — обновить
`progress.md` и этот файл, сделать git-commit.

- [x] **Фаза 0** — каркас Next.js + Tailwind v4 + shadcn/ui + git, папки, `CLAUDE.md` и `/docs`,
  форма mock-данных, абстракция `lib/commerce.ts`, подготовка к Vercel. *Токены одобрены.*
- [x] **Фаза 1** — дизайн-токены (`@theme`) + базовый layout: header, footer, навигация.
  circular-font исправлен; шрифты **Pirata One / Oswald / Golos Text**. *Подтверждено визуально.*
- [x] **Фаза 2** — главная: hero + промо-секции (value-props, manifesto, order-CTA). *Готово.*
- [x] **Фаза 3** — страница коллекции `/products` (сетка) + карточка товара + фото в `public/products/`. *Готово.*
- [x] **Фаза 4** — страница товара `/products/[handle]`: галерея, «легенда», выбор размера,
  добавление в клиентскую корзину (CartProvider + счётчик). *Готово.*
- [x] **Фаза 5** — корзина-дровер (`Sheet` справа): позиции, qty ±, удаление, subtotal, пустое
  состояние; вместо чекаута — CTA «Замовити в Direct». Открытие по клику на корзину и при добавлении. *Готово.*
- [x] **Фаза 6** — страницы FAQ / About / Lookbook + навигация. Анимация печати на главной
  (вращение + появление hero), раскладка Lookbook (пэкшоты в центре, образы по бокам). *Готово.*
- [x] **Фаза 7** — SEO (metadataBase, OG-картинка, sitemap, robots), перформанс (LCP `priority`),
  a11y (skip-link, фокус), брендовая 404, адаптив-полировка, фикс круговой печати в Safari.
  **Задеплоено на Vercel:** https://softishopdemobild.vercel.app (репо GitHub: karateka004/SoftiShop,
  авто-деплой при push). *Готово.*

Легенда: `[ ]` не начато · `[~]` в работе/на ревью · `[x]` готово.
